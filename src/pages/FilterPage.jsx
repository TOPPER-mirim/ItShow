import React, { useState } from "react";
import { useNavigate, useLocation } from "react-router-dom";

const filters = ["가오", "감성", "재미"];

function FilterPage() {
    return (
        <div>Filter Page</div>
    );
}

export default FilterPage;